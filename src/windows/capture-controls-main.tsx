import React from 'react';
import ReactDOM from 'react-dom/client';
import CaptureControlsWindow from './CaptureControlsWindow';
import '../styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CaptureControlsWindow />
  </React.StrictMode>
);
